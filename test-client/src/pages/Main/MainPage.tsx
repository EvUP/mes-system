import React from 'react';
import { Typography } from '@mui/material';

export default function MainPage(): React.JSX.Element {
  return <Typography variant="h4">Добро пожаловать в MES-систему!</Typography>;
}
